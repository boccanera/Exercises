package Books.CleanCode.Misc;

public class EnviromentControlTest {

/*
Uppercase = "ON" e as lowercase = "OFF" (heater, blower, cooler, hi-temp- alarm, lo-temp-alarm}.

@Test
public void turnOnCoolerAndBlower IfTooHot() throws Exception { tooHot();
        assertEquals("hBChl", hw.getState());
@Test
public void turnOnHeaterAndBlower IfTooCold() throws Exception { tooCold();
        assertEquals("HBchl", hw.getState());
@Test
public void turnOnHiTempAlarmAt Threshold() throws Exception { way TooHot();
        }
        assertEquals("hBCHI", hw.getState());
@Test
public void turnOnLoTempAlarmAt Threshold() throws Exception [ wayTooCold();
        1
        assertEquals("HBchL", hw.getState());


public String getState() { String state = "";
state += heater ? "H": "h": state blower ? "B": "b"; state += cooler ? "C": "c":
state+hiTempAlarm ? "H" : "h"; state + loTempAlarm? "L": "1"; return state;
*/

}

